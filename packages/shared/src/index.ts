/**
 * @projection-mapper/shared
 *
 * Shared protocol, API contract and domain types used by both the desktop
 * application and the mobile companion app.
 */

export * from './remote-protocol';
export * from './api';
